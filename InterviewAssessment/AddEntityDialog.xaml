<Window x:Class="DomainModelEditor.AddEntityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DomainModelEditor"
        mc:Ignorable="d"
        Title="Add New Entity" Height="130" Width="450">
    <StackPanel>
        <StackPanel>
            <Label>Name of the new entity</Label>
            <TextBox x:Name="Name" Margin="10"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Click="Add_Click" Width="50" />
            <Button Content="Cancel" Click="Cancel_Click" Margin="10,0" Width="50" />
        </StackPanel>
    </StackPanel>
</Window>
